import React from 'react';

const Header = () => {
  return (
    
      <div>
        <select>
          <option>Select by Gateway</option>
        </select>
      </div>
    
  );
};

export default Header;